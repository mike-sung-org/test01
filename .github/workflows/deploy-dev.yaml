name: Deploy Development

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: 
      name: Development
    steps:
      - name: deploy
        id: deploy
        run: |
          echo "deployment completed!"
