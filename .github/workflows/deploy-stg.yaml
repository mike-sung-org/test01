name: Deploy Staging

on:
  push:
    branches:
      - master

jobs:
  deployment:
    runs-on: ubuntu-latest
    env:
      TEST_VAR: ${{ vars.TEST_VAR }}
    steps:
      - name: deploy
        run: |
          echo "deployment succeeded! TEST_VAR=${TEST_VAR}"
